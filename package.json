{
  "name": "smartmeal",
  "version": "1.0.0",
  "scripts": {
    "install-all": "cd client && npm install && cd ../admin && npm install && cd ../server && npm install",
    "start": "concurrently \"cd client && npm start\" \"cd admin && npm start\" \"cd server && npm start\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
